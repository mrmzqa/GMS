
<UserControl x:Class="GMS25.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GMS25.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Product List -->
        <ListView ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Price" DisplayMemberBinding="{Binding Price, StringFormat=C}"/>
                    <GridViewColumn Header="Stock" DisplayMemberBinding="{Binding StockQuantity}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Product Details -->
        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Product Details" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Name:" Grid.Row="0" Grid.Column="0" Margin="0,0,5,5"/>
                <TextBox Text="{Binding SelectedProduct.Name}" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5"/>

                <TextBlock Text="Description:" Grid.Row="1" Grid.Column="0" Margin="0,0,5,5"/>
                <TextBox Text="{Binding SelectedProduct.Description}" Grid.Row="1" Grid.Column="1" Margin="0,0,0,5"/>

                <TextBlock Text="Price:" Grid.Row="2" Grid.Column="0" Margin="0,0,5,5"/>
                <TextBox Text="{Binding SelectedProduct.Price}" Grid.Row="2" Grid.Column="1" Margin="0,0,0,5"/>

                <TextBlock Text="Stock:" Grid.Row="3" Grid.Column="0" Margin="0,0,5,5"/>
                <TextBox Text="{Binding SelectedProduct.StockQuantity}" Grid.Row="3" Grid.Column="1" Margin="0,0,0,5"/>

                <TextBlock Text="Category:" Grid.Row="4" Grid.Column="0" Margin="0,0,5,5"/>
                <ComboBox ItemsSource="{Binding Categories}" 
                          DisplayMemberPath="Name"
                          SelectedValuePath="CategoryId"
                          SelectedValue="{Binding SelectedProduct.CategoryId}"
                          Grid.Row="4" Grid.Column="1" Margin="0,0,0,10"/>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="New" Command="{Binding AddProductCommand}" Margin="0,0,5,0"/>
                <Button Content="Save" Command="{Binding SaveProductCommand}" Margin="0,0,5,0"/>
                <Button Content="Delete" Command="{Binding DeleteProductCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
