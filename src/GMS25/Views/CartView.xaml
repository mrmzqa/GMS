<UserControl x:Class="GMS25.Views.CartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GMS25.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Cart Items -->
        <ListView ItemsSource="{Binding CartItems}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Product" DisplayMemberBinding="{Binding Product.Name}"/>
                    <GridViewColumn Header="Price" DisplayMemberBinding="{Binding Product.Price, StringFormat=C}"/>
                    <GridViewColumn Header="Quantity" DisplayMemberBinding="{Binding Quantity}"/>
                    <GridViewColumn Header="Total" DisplayMemberBinding="{Binding Product.Price, Converter={StaticResource MultiplyConverter}, ConverterParameter={Binding Quantity}, StringFormat=C}"/>
                    <GridViewColumn Header="Action">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Remove" Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Checkout Panel -->
        <Border Grid.Row="1" Background="#EEE" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Total:" FontWeight="Bold" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding Total, StringFormat=C}" FontWeight="Bold" FontSize="16" Margin="0,0,20,0" VerticalAlignment="Center"/>
                <Button Content="Checkout" Command="{Binding CheckoutCommand}" Padding="20,5" FontSize="14"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>


