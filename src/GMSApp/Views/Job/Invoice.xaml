<UserControl x:Class="GMSApp.Views.Job.Invoice"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100">

    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="340"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Invoice list -->
        <Border Grid.Column="0" Padding="8" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Background="WhiteSmoke">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0 0 0 8">
                    <Button Content="Reload" Command="{Binding LoadCommand}" Width="70" Margin="0,0,6,0"/>
                    <Button Content="New" Command="{Binding AddCommand}" Width="70" Margin="0,0,6,0"/>
                    <Button Content="Save" Command="{Binding SaveCommand}" Width="70" Margin="0,0,6,0"/>
                    <Button Content="Delete" Command="{Binding DeleteCommand}" Width="70" />
                </StackPanel>

                <DataGrid ItemsSource="{Binding Invoices}"
                          SelectedItem="{Binding SelectedInvoice, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Inv #" Binding="{Binding InvoiceNumber}" Width="*"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding InvoiceDate, StringFormat=\{0:yyyy-MM-dd\}}" Width="120"/>
                        <DataGridTextColumn Header="Vendor" Binding="{Binding Vendor.Name}" Width="140"/>
                        <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=N2}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch" ShowsPreview="True" />

        <!-- Right: Invoice details & lines -->
        <Border Grid.Column="2" Padding="12" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header fields -->
                <StackPanel Orientation="Horizontal" Grid.Row="0">
                    <StackPanel Width="520">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <TextBlock Text="Invoice #" Width="110" VerticalAlignment="Center"/>
                            <TextBox Text="{Binding SelectedInvoice.InvoiceNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                            <TextBlock Text="Date:" Width="40" Margin="12,0,0,0" VerticalAlignment="Center"/>
                            <DatePicker SelectedDate="{Binding SelectedInvoice.InvoiceDate, Mode=TwoWay}" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <TextBlock Text="Vendor:" Width="110" VerticalAlignment="Center"/>
                            <ComboBox x:Name="VendorCombo"
                                      ItemsSource="{Binding Vendors}"
                                      SelectedItem="{Binding SelectedVendor, Mode=TwoWay}"
                                      DisplayMemberPath="Name"
                                      Width="260"
                                      DropDownOpened="VendorCombo_DropDownOpened"/>
                            <Button Content="Vendors" Width="70" Margin="8,0,0,0" Command="{Binding LoadVendorsCommand}"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <TextBlock Text="Purchase Order:" Width="110" VerticalAlignment="Center"/>
                            <ComboBox x:Name="POCombo"
                                      ItemsSource="{Binding PurchaseOrders}"
                                      SelectedItem="{Binding SelectedPurchaseOrder, Mode=TwoWay}"
                                      DisplayMemberPath="PONumber"
                                      Width="260"
                                      DropDownOpened="POCombo_DropDownOpened"/>
                            <Button Content="POs" Width="70" Margin="8,0,0,0" Command="{Binding LoadPurchaseOrdersCommand}"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Margin="24,0,0,0" Width="300">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <TextBlock Text="SubTotal:" Width="110" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding SelectedInvoice.SubTotal, StringFormat=N2}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <TextBlock Text="Discount:" Width="110" VerticalAlignment="Center"/>
                           