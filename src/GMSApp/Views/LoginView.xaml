<UserControl x:Class="GarageApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d">
    <Grid Margin="100">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Login" FontSize="32" Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20" />
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Username"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Width="250"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <Label Content="Password"/>
            <PasswordBox Width="250" x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" />
        </StackPanel>
        <StackPanel Grid.Row="3" HorizontalAlignment="Center">
            <Button Content="Login" Width="150" Height="30" Command="{Binding LoginCommand}" />
            <TextBlock Foreground="Red" Margin="0,5,0,0" Text="{Binding ErrorMessage}" />
        </StackPanel>
    </Grid>
</UserControl>