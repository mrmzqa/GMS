<UserControl x:Class="GmsApp.Views.JobContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GmsApp.Views" Height="Auto" Width="Auto"
             >
    <Grid Margin="1">
        
        <TabControl>
            <TabItem Header="Menu">
                <TreeView RenderTransformOrigin="0.504,2.285" Margin="0,44,1,-44">
                    <Button Content="Quotation" Margin="5"/> 
                    <Button Content="Joborder" Click="Jo_Click" Margin="5" /> 
                    <Button Content="Purchaseorder" Click="Po_Click" Margin="5"/> 
                    <Button Content="Invoice" Margin="5"/> 
                </TreeView>
            </TabItem>
            <TabItem Header="Quotation">
                <ContentControl x:Name="QuotationContent"
Height="Auto" Width="Auto" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
            </TabItem>
            <TabItem Header="JobOrder">
                <ContentControl x:Name="JobContent"
            Height="Auto" Width="Auto" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
            </TabItem>
            <TabItem Header="Purchaseorder">
                <ContentControl x:Name="POContent"
Height="Auto" Width="Auto" FontSize="9"/>
            </TabItem>
            <TabItem Header="InVoice">
                <ContentControl x:Name="IVContent"
Height="Auto" Width="Auto" FontSize="9"/>
            </TabItem>
           
            
        </TabControl>
      

    </Grid>
</UserControl>