<Window x:Class="GMSApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Garage Manager" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Form -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="120" Margin="5,0" Text="{Binding SelectedVehicle.Make, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Width="120" Margin="5,0" Text="{Binding SelectedVehicle.Model, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Width="120" Margin="5,0" Text="{Binding SelectedVehicle.LicensePlate, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Add" Margin="5,0" Click="Add_Click"/>
            <Button Content="Update" Margin="5,0" Click="Update_Click"/>
            <Button Content="Delete" Margin="5,0" Click="Delete_Click"/>
            <TextBox x:Name="SearchBox" Width="120" Margin="10,0" />

        </StackPanel>

        <!-- Data Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Vehicles}" SelectedItem="{Binding SelectedVehicle, Mode=TwoWay}" 
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Make" Binding="{Binding Make}" Width="*"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="*"/>
                <DataGridTextColumn Header="Plate" Binding="{Binding LicensePlate}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
