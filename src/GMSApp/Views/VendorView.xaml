<!-- VendorView.xaml -->
<UserControl x:Class="GMSApp.Views.VendorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="520" d:DesignWidth="900">

    <!--
        DataContext should be set to VendorViewModel by the host (window or DI container).
        Expects:
         - ObservableCollection<Vendor> Vendors
         - Vendor? SelectedVendor
         - ICommand LoadCommand, AddCommand, SaveCommand, DeleteCommand
    -->

    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: list -->
        <Border Grid.Column="0" Padding="8" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Background="WhiteSmoke">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0 0 0 8">
                    <Button Content="Reload" Command="{Binding LoadCommand}" Width="70" Margin="0,0,6,0"/>
                    <Button Content="New" Command="{Binding AddCommand}" Width="70" Margin="0,0,6,0"/>
                    <Button Content="Save" Command="{Binding SaveCommand}" Width="70" Margin="0,0,6,0"/>
                    <Button Content="Delete" Command="{Binding DeleteCommand}" Width="70" />
                </StackPanel>

                <DataGrid ItemsSource="{Binding Vendors}"
                          SelectedItem="{Binding SelectedVendor, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="False"
                          Margin="0"
                          MinHeight="300">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="160"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch" ShowsPreview="True" />

        <!-- Right: details -->
        <Border Grid.Column="2" Padding="12" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Vertical">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Name:" Width="120" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding SelectedVendor.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="320"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Contact Person:" Width="120" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding SelectedVendor.ContactPerson, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="320"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Phone:" Width="120" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding SelectedVendor.Phone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="180"/>
                        <TextBlock Text="CR #" Width="60" Margin="12,0,0,0" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding SelectedVendor.CRNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="140"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Email:" Width="120" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding SelectedVendor.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="320"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Width="90" Margin="0,0,6,0" />
                    <Button Content="Save" Command="{Binding SaveCommand}" Width="100" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>