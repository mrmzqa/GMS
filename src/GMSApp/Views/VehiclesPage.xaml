<Window x:Class="GMSApp.Views.VehiclesPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vm="clr-namespace:GMSApp.ViewModels"
        Title="Vehicles" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="100" Text="{Binding SelectedVehicle.LicensePlate, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Width="100" Text="{Binding SelectedVehicle.Make, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Width="100" Text="{Binding SelectedVehicle.Model, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Width="60" Text="{Binding SelectedVehicle.Year, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Add" Command="{Binding AddVehicleCommand}" Margin="5,0"/>
            <Button Content="Update" Command="{Binding UpdateVehicleCommand}" Margin="5,0"/>
            <Button Content="Delete" Command="{Binding DeleteVehicleCommand}" Margin="5,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Vehicles}" SelectedItem="{Binding SelectedVehicle}" AutoGenerateColumns="True"/>
    </Grid>
</Window>
