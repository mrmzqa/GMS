<UserControl x:Class="GMSApp.Views.VehiclePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:GMSApp.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <conv:ByteArrayToImageConverter x:Key="ByteArrayToImageConverter"/>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left list panel -->
        <StackPanel Grid.Column="0">
            <Button Content="New Vehicle" Click="NewVehicle_Click" Margin="0,0,0,10"/>
            <ListBox ItemsSource="{Binding Vehicles}" SelectedItem="{Binding SelectedVehicle}"
                     DisplayMemberPath="Make" Height="500" />
        </StackPanel>

        <!-- Right details & actions panel -->
        <StackPanel Grid.Column="1" Margin="10,0,0,0">
            <TextBlock Text="Vehicle Details" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Make:" Width="80" VerticalAlignment="Center"/>
                <TextBox Text="{Binding SelectedVehicle.Make, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Model:" Width="80" VerticalAlignment="Center"/>
                <TextBox Text="{Binding SelectedVehicle.Model, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Year:" Width="80" VerticalAlignment="Center"/>
                <TextBox Text="{Binding SelectedVehicle.Year, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="VIN:" Width="80" VerticalAlignment="Center"/>
                <TextBox Text="{Binding SelectedVehicle.VIN, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
            </StackPanel>

            <TextBlock Text="Vehicle Image:" Margin="0,10,0,5" />

            <Border BorderBrush="Gray" BorderThickness="1" Width="200" Height="150" Margin="0,0,0,10">
                <Image Stretch="Uniform"
                       Source="{Binding SelectedVehicle.Image, Converter={StaticResource ByteArrayToImageConverter}}"/>
            </Border>

            <Button Content="Upload Image" Command="{Binding UploadImageCommand}" Width="120" Margin="0,0,0,20" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" >
                <Button Content="Save" Command="{Binding AddOrUpdateVehicleCommand}" Width="100" Margin="0,0,10,0"/>
                <Button Content="Delete" Command="{Binding DeleteVehicleCommand}" Width="100"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>