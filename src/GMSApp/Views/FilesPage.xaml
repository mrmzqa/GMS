<UserControl x:Class="GMSApp.Views.FilesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:GMSApp.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- File list and controls -->
        <StackPanel Grid.Column="0">
            <Button Content="Upload File" Command="{Binding UploadFileCommand}" Margin="0,0,0,5"/>
            <Button Content="Delete Selected" Command="{Binding DeleteFileCommand}" Margin="0,0,0,10"/>
            <ListBox ItemsSource="{Binding Files}" SelectedItem="{Binding SelectedFile}" DisplayMemberPath="FileName" />
        </StackPanel>
        <StackPanel Grid.Column="0">
            <Button Content="Upload File" Command="{Binding UploadFileCommand}" Margin="0,0,0,5"/>
            <Button Content="Delete Selected" Command="{Binding DeleteFileCommand}" Margin="0,0,0,10"/>
            <ListBox ItemsSource="{Binding Files}" SelectedItem="{Binding SelectedFile}" DisplayMemberPath="FileName" />
        </StackPanel>

        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="10,0,0,0">
            <ContentControl x:Name="FileViewer"/>
        </Border>

      
    </Grid>
</UserControl>


